# Title

## Usage

1. `cd` into the ??? directory.
2. Build with `cargo build`.
3. **(optional)** Run `??? fetch-market-data` to download the latest market data. This is optional as the software is packaged with data by default. The data takes a few minutes to download.
